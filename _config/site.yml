profiles:
  production:
    base_url: http://feelpp.github.io/www.feelpp.org
    deploy:
      host: github_pages
      # use gh-branch for repositories other than <username>.github.io
      branch: gh-pages
      # use master branch for <username>.github.io repository
      #branch: master
